name: Deploy a Vercel

on:
  workflow_dispatch:
  workflow_run:
    workflows: [CI]
    branches: [main]
    types: [completed]

permissions:
  contents: read

jobs:
  deploy-backend:
    name: Backend / production deploy after green CI
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment: production
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - run: npm ci
      - run: npm install --global vercel@58.9.0
      - name: Link production project
        run: vercel link --yes --project fitmanager-backend --scope progra2 --token '${{ secrets.VERCEL_TOKEN }}'
      - name: Synchronize Supabase connection
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: vercel env update DATABASE_URL production --value "$SUPABASE_DB_URL" --yes --token '${{ secrets.VERCEL_TOKEN }}'
      - name: Synchronize public app URL for transactional emails
        run: vercel env update PUBLIC_APP_URL production --value 'https://fitmanager-saas.vercel.app' --yes --token '${{ secrets.VERCEL_TOKEN }}'
      - name: Synchronize frontend origins for CORS
        run: vercel env update FRONTEND_URLS production --value 'https://fitmanager-saas.vercel.app,https://frontend-progra2.vercel.app,https://frontend-git-main-progra2.vercel.app' --yes --token '${{ secrets.VERCEL_TOKEN }}'
      - name: Deploy production
        run: vercel deploy --prod --yes --token '${{ secrets.VERCEL_TOKEN }}'
      - name: Post-deploy smoke
        run: curl --fail --show-error --silent --retry 5 --retry-delay 5 https://fitmanager-backend-nine.vercel.app/api/health
